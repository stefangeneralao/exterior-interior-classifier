{
  "name": "exterior_interior",
  "version": "1.0.0",
  "license": "MIT",
  "scripts": {
    "start": "python3 server.py",
    "build-docker": "docker build -t extint_classifier .",
    "run-docker": "docker run -p 3002:3002 -d --name extint_classifier extint_classifier",
    "rm-docker": "docker stop extint_classifier && docker rm extint_classifier"
  }
}
